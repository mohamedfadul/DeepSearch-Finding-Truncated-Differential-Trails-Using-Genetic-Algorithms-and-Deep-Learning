counter = 0
for files in range (0,999):
	f = open(r"files location"+ str(files) + ".txt", "r")

	# Search for the given string
	for line in f:
		if "Characteristic" in line:
			counter=counter+1
	f.close()
print(counter)
